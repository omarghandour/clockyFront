/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://clockyeg.com", // Change this to your domain
  generateRobotsTxt: true, // Automatically generate robots.txt
  sitemapSize: 5000, // Optional, useful for large sites
};
